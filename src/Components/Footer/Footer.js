import './style.less';

const Footer = () =>
    <footer className="Footer">
        It uses Github Actions to help with deploy and static HTML version.<br/>
        API is build with node.js code and stored as flat JSON and Markdown files on S3 bucket.
    </footer>

export default Footer;